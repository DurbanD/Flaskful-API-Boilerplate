def send(sendTo, tempToken):
    # Generate an email containing a URL with the temporary access token in a query
    # Use the token to send a POST to update with the new password on the front end
    return